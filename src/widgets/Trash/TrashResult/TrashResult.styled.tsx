import styled from 'styled-components';

export const TrashGroup = styled.div`
	margin-bottom: 32px;
`;

export const TrashList = styled.ul`
	margin: 0;
	padding: 0;
	list-style: none;
	display: grid;
	grid-template-columns: repeat(2, 1fr);
	gap: 0 16px;
`;
