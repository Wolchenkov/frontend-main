import styled from 'styled-components';
import { Tabs } from 'reshaped/bundle';

export const ProjectTabList = styled(Tabs.List)`
	div[role='tablist'] {
		background: var(--rs-color-background-neutral);
	}
`;
