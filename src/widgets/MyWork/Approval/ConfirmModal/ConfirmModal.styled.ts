import styled from 'styled-components';
import { Text } from 'reshaped/bundle';

export const ConfirmModalText = styled(Text)`
	font-weight: 500;
	letter-spacing: -0.02em;
`;

export const ConfirmModalDetailedText = styled(Text)`
	padding-top: 12px;
	letter-spacing: -0.02em;
`;
